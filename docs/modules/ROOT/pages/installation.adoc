= Installation

Add the dependency to your `shard.yml`:

[source,yaml]
----
dependencies:
  kramdown-asciidoc:
    github: aloli-crystal/kramdown-asciidoc
    version: "~> 2.1.1.4"
----

Then run `shards install`.
